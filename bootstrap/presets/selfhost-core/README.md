# @PROJECT_NAME@ self-hosting core workspace

This package is organized for **real future self-hosting work**, not as a claim that a fixed-point compiler already exists. `src/main.hf` and `tests/smoke.hf` form the verified Stage-0 build path. The small `compiler/` units are explicit, standalone starting points for a future linked Stage-1 compiler core.

The current repository validates self-hosted compiler states 1–9 as deterministic milestone programs. It does not yet link these units into a fixed-point Stage-1 compiler, and this preset keeps that distinction visible.
