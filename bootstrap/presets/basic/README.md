# @PROJECT_NAME@

This is a minimal **Holy Fitra v1 Stage-0** project. It is suitable for the verified scalar subset: integers, booleans, functions, local bindings, control flow, fixed arrays, structs, and bounded runtime helpers.

Run `holyfitra-v1.sh check .`, `holyfitra-v1.sh test .`, or `holyfitra-v1.sh build . -o build/@PROJECT_NAME@` from a directory that contains the v1 toolchain.
